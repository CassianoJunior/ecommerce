import { Flex, HStack } from '@chakra-ui/react';

const Navbar = () => (
  <Flex>
    <IconButton />
    <Flex>imagem</Flex>
    <Flex>
      <HStack />
    </Flex>
  </Flex>
);

export default Navbar;
